export class Person {
  public id: string;
  public name: string;
  public title: string;

  constructor(id: string, name: string, title: string) {
    this.id = id;
    this.name = name;
    this.title = title;
  }
}
